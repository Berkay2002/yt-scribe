# Codex Harness

Use this file when `yt-scribe` invokes Codex through `codex exec`.

The transcript is provided through stdin, appended to the prompt as the content to transform. Return only the polished transcript output.

Do not mention Codex, stdin, or the polishing process in the final answer.
