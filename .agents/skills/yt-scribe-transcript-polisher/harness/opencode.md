# OpenCode Harness

Use this file when `yt-scribe` invokes OpenCode through `opencode run`, usually with `--agent yt-scribe-transcript-polisher`.

The transcript is attached as a temp transcript file. Read that attached transcript as the content to transform. Return only the polished transcript output.

Do not mention OpenCode, the attached file, or the polishing process in the final answer.
